float calculatePWR(float voltage,float current) //calculate power
{
  return 0.0;
}

float calculatePWRP(float power) //calculate price for power consumption
{
  return 0.0;
}

float calculatePrintingCost(float manPowerCost,float powerCost,float materialCost)
{
  return 0.0;
}

float currencyConversion(float value, float exchangeRate)
{
  return 0.0;
}
